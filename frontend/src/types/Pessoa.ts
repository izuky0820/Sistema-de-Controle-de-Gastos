/*
 * Representa uma pessoa cadastrada no sistema.
 *
 * Essa interface corresponde ao modelo Pessoa
 * existente no backend.
 */
export interface Pessoa{
    id:number;
    nome:string;
    idade:number;
}